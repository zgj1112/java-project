spring:
  application:
    name: first-web-manager

  #数据库配置
  datasource:
    url: jdbc:mysql://localhost:3306/myproject?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

#mybatis配置
mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
